<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>キャンプ場編集</title>
</head>
<body>
    <h1>キャンプ場編集</h1>
    <form action="/campgrounds/<%=campground._id%>?_method=PUT" method="post">
        <div>
            <label for="title">タイトル</label>
            <input type="text" name="campgrounds[title]" id="title" value="<%= campground.title %>">
        </div>
        <div>
            <label for="location">ロケーション</label>
            <input type="text" name="campgrounds[location]" id="location" value="<%= campground.title %>">
        </div>
        
        <button>編集する</button>
   
    
    </form>
    <a href="/campgrounds">一覧に戻る</a>
</body>
</html>